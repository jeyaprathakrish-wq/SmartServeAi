'use client';
import { useState } from 'react';
import { createClient } from '@/lib/supabase/browser';
export function StaffLogin({ role }: { role:'KITCHEN'|'MANAGER' }) { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); async function login(){const supabase=createClient();const result=await supabase.auth.signInWithPassword({email,password});if(result.error)return setError(result.error.message); window.location.href=role==='KITCHEN'?'/kitchen':'/manager';} return <main className="container"><section className="card" style={{maxWidth:440,margin:'60px auto'}}><h1>{role==='KITCHEN'?'Kitchen':'Manager'} login</h1><p style={{color:'var(--muted)'}}>Authentication is handled by Supabase Auth. No credentials are bundled in the app.</p><input className="input" placeholder="Work email" value={email} onChange={e=>setEmail(e.target.value)} style={{marginBottom:10}}/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p style={{color:'var(--danger)'}}>{error}</p>}<button className="button" style={{width:'100%',marginTop:14}} onClick={login}>Sign in</button></section></main>; }
